# Copy this file to .env and fill in your real credentials.
# .env is git-ignored.
#
# Tests hit the frontend container's own port, which bypasses the Authentik
# SSO proxy in front of the public hostname, so the normal login works.
BASE_URL=http://localhost:8093
TEST_EMAIL=you@example.com
TEST_PASSWORD=your-password
